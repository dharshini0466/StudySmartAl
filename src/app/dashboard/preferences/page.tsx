import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PreferencesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Preferences</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This page is under construction. User preferences and settings will be available here.</p>
      </CardContent>
    </Card>
  );
}
